var numbers = [1, 2, 3];

function getMax(numbers){
    var max = numbers[0];

    for (var = 1; i=number.length; i++){
        if (numbers[1]> max){
            max = numbers[i];

        }
    }
}

return max;