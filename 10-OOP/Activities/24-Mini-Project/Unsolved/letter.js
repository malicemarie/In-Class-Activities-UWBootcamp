"use strict";

class Letter {
  constructor(character) {
    this.character = character;
    this.visible = !(character >= "A" && value[i] <= "Z");
  }
  isCorrect(){
      if (this.character === character){
          toVisible();
      }
  }
  toVisible()
}

module.exports = Letter;
